<%- include("partials/head", { title: "Dashboard", css: ["/css/base.css"] }) %>

  <body>
    <!-- Dashboard page: shows user progress, budget status, and quick navigation -->
    <!-- Navbar -->
    <%- include("partials/navbar") %>

    <!-- Greeting + Progress -->
    <div class="container mt-5">
      <div class="row g-4">
        <div class="col-12">
          <div class="card p-5 app-card text-center">
            <h2>Hello, <%= user.name %> 👋</h2>

            <p class="mt-3 fs-5">
              You completed
              <span id="habit-progress">0/0</span> habits today
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Budget -->
    <div class="container mt-5">
      <div class="row g-4">
        <div class="col-12">
          <div class="card p-5 app-card text-center">
            <h4>Remaining Budget</h4>
            <h2 id="dashboard-budget">৳ 0</h2>

            <span
              id="budget-warning"
              class="badge mt-2"
              style="display: none"
            ></span>
          </div>
          <div class="progress mt-3">
            <div id="budget-bar" class="progress-bar"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Navigation Buttons -->
    <div
      class="container mt-5 mb-4 d-flex justify-content-center gap-4 flex-wrap"
    >
      <a href="/habits" class="btn btn-outline-success px-4">Add Habits</a>
      <a href="/expenses" class="btn btn-outline-warning px-4">Show Expenses</a>
      <a href="/" class="btn btn-outline-danger px-4">Go to Home</a>
      <a href="/leaderboard" class="btn btn-outline-info px-4"
        >🏆 Leaderboard</a
      >

      <a href="/savings" class="btn btn-outline-success px-4">Savings</a>
    </div>

    <!-- Footer -->
    <%- include("partials/footer") %>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
    <script type="module">
      import { initDashboard } from "/js/dashboard.js";
      initDashboard();
    </script>
  </body>
</html>
